<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:editor="clr-namespace:WpfControl.Editor"
>
  <!--  MatrixViewer  -->
  <Style TargetType="{x:Type editor:MatrixViewer}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type editor:MatrixViewer}">
          <ScrollViewer
              CanContentScroll="True"
              HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
              VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
          >
            <editor:MatrixDisplay
                x:Name="PART_MatrixDisplay"
                Columns="{Binding Columns, RelativeSource={RelativeSource TemplatedParent}}"
                ColumnWidths="{Binding ColumnWidths, RelativeSource={RelativeSource TemplatedParent}}"
                RowHeights="{Binding RowHeights, RelativeSource={RelativeSource TemplatedParent}}"
                Rows="{Binding Rows, RelativeSource={RelativeSource TemplatedParent}}"
            />
          </ScrollViewer>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
